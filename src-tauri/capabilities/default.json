{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the local Chenglu admin desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
